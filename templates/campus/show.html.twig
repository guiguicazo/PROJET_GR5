{% extends 'base.html.twig' %}

{% block title %}{{ campus.nom }}{% endblock %}

{% block body %}
    <h1 style="color: #FFFFFF">{{ campus.nom }}</h1>

    <table class="table" style="color: #FFFFFF">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ campus.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ campus.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a style="color: #FFFFFF" href="{{ path('app_campus_index') }}">Retour à la liste</a>

    <a href="{{ path('app_campus_edit', {'id': campus.id}) }}" style="color: #FFFFFF">Modifier</a>

    {{ include('campus/_delete_form.html.twig') }}
{% endblock %}
