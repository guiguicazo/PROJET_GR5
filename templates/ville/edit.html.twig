{% extends 'base.html.twig' %}

{% block title %}Edit Ville{% endblock %}

{% block body %}
    <div class="container"><h1 style="padding-top: 2rem; padding-bottom: 5rem; color: #FFFFFF">{{ ville.nom }}</h1>
        <div class="row">
            <div class="card text-center w-100 align-middle">
                <div class="card-body">
                    <h1>Modifier la ville</h1>

                    {{ include('ville/_form.html.twig', {'button_label': 'Mettre a jour'}) }}

                    <a href="{{ path('app_ville_index') }}">Retour a la liste</a>

                    {{ include('ville/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
