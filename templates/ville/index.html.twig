{% extends 'base.html.twig' %}

{% block title %}Ville index{% endblock %}

{% block body %}
    <h1 style="color: #FFFFFF">Liste de ville</h1>
    {{ include('ville/filterVille.html.twig', {'button_label': 'Rechercher'}) }}
    <table class="table" style="color: #FFFFFF">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>CodePostal</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ville in villes %}
            <tr>
                <td>{{ ville.id }}</td>
                <td>{{ ville.nom }}</td>
                <td>{{ ville.codePostal }}</td>
                <td>
                    <a class="text-success" href="{{ path('app_ville_show', {'id': ville.id}) }}">Détail</a>
                    <a class="text-primary" href="{{ path('app_ville_edit', {'id': ville.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Pas de ville trouver</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ville_new') }}">Creer une nouvelle ville</a>
{% endblock %}
