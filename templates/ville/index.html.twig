{% extends 'base.html.twig' %}

{% block title %}Ville index{% endblock %}

{% block body %}
    <div class="container"><h1 style="color: #FFFFFF; padding-top: 2rem; padding-bottom: 5rem">Liste des villes</h1>
        <div class="row">
            <div class="card text-center w-100 align-middle">
                <div class="card-body">
                    {{ include('ville/filterVille.html.twig', {'button_label': 'Rechercher'}) }}
                    <table class="table" style=>
                        <thead>
                        <tr>
                            <th>Id</th>
                            <th>Nom</th>
                            <th>CodePostal</th>
                            <th>actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for ville in villes %}
                            <tr>
                                <td>{{ ville.id }}</td>
                                <td>{{ ville.nom }}</td>
                                <td>{{ ville.codePostal }}</td>
                                <td>
                                    <a class="text-success"
                                       href="{{ path('app_ville_show', {'id': ville.id}) }}">Détail</a>
                                    <a class="text-primary" href="{{ path('app_ville_edit', {'id': ville.id}) }}">Modifier</a>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="4">Pas de ville trouver</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>

                    <a href="{{ path('app_ville_new') }}">Creer une nouvelle ville</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
